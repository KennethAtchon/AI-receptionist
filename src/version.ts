/**
 * SDK Version - Auto-imported from package.json
 * This ensures the version always matches package.json
 */

import packageJson from '../package.json';

export const SDK_VERSION = packageJson.version;

